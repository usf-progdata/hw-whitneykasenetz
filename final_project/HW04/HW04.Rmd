---
title: "HW04"
author: "Whitney Kasenetz"
date: "3/16/2021"
output: html_document
---

<!-- Add some commentary -->

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(here)
library(readr)
library(dplyr)

# WDG: Where is pdiQuestions coming from? It seems to just appear out of nowhere.
write_csv(pdiQuestions, 
          here::here("final_project", "data", "pdiQuestions.csv")
          
dat <- read_csv(here::here( "data", "pdiQuestions.csv"))

data(dat)
str(dat)

```


```{r}
# WDG: Make sure to include plots


# WDG: We have to write (write.csv()) and save (ggsave()) things.

```
